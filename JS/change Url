        var searchParams = new URLSearchParams(window.location.search)
        searchParams.set("subject", jQuery(this).val());
        var newRelativePathQuery = removeParams('subject');
        history.pushState(null, '', newRelativePathQuery);
